name: show_data
description: Show data

inputs:
- name: input

implementation:
  container:
    image: ghcr.io/dkmiller/tidbits/show_data:latest

    command: [
      python,
      run.py,
      --input,
      {inputPath: input}
    ]
